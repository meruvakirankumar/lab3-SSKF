[project]
name = "clients"
version = "0.1.0"
description = "Internal shared clients"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",

    "couchbase>=4.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
